name: ML Client CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: machine-learning-client

    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t priority-manager-ml-client .
