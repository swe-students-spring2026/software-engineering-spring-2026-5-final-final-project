services:
  web:
    build: ./web
    ports:
      - "5000:5000"
    env_file:
      - ./web/.env
    depends_on:
      - mongo

  mongo:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
